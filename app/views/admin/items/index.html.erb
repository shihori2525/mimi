商品一覧
<%= link_to "商品登録",new_admin_item_path %>
<teble>
  <tbody>
    <% @items.each do |item| %>
    <tr>
      <td>
      <%= link_to admin_item_path(item) do %>
       <%= attachment_image_tag item, :item_image, format: 'jpeg', fallback: "no_image.jpg", size: "40x40" %>
      <% end %>
      </td>
      <td>
      <%= link_to admin_item_path(item) do %>
        <%= item.name %>
      <% end %>
      </td>
      <td><%= link_to "編集",edit_admin_item_path(item) %></td>
      <td><%= link_to "削除",admin_item_path(item) ,method: :delete,"data-confirm" => "本当に消しますか？"%></td>
    </tr>
    <% end %>
  </tbody>
</teble>